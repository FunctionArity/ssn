<div class="py-8">
  <%= render @guard %>

  <div class="mt-6 flex items-center gap-4">
    <%= link_to t(".edit"), edit_guard_path(@guard), class: "text-blue-600 hover:underline" %>
    <%= link_to t(".back"), guards_path, class: "text-blue-600 hover:underline" %>
    <%= button_to t(".destroy"), @guard, method: :delete, class: "cursor-pointer rounded-md bg-red-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-red-500" %>
  </div>
</div>
