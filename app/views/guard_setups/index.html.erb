<div>
  <%= render "shared/page_header",
        title: t(".title"),
        icon: "ph-shield",
        icon_class: "bg-blue-100 text-blue-700",
        action_label: policy(GuardSetup).new? ? t(".new_guard_setup") : nil,
        action_path: policy(GuardSetup).new? ? new_guard_setup_path : nil %>

  <%= render "guard_setups_grid", guard_setups: @guard_setups %>
</div>
