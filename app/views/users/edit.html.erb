<div class="py-8">
  <h1 class="text-2xl font-bold mb-6"><%= t(".title") %></h1>

  <%= render "form", user: @user %>

  <div class="mt-6 flex gap-4">
    <%= link_to t(".show"), @user, class: "text-blue-600 hover:underline" %>
    <%= link_to t(".back"), users_path, class: "text-blue-600 hover:underline" %>
  </div>
</div>
